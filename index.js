/**
 * dsh-backup-btn — host entry (minimal).
 * The client half (client.js) is auto-injected by DSH via exports["./client"].
 */
export const name = 'dsh-backup-btn'

export async function apply() {
  // no-op: UI work happens in the client bundle
}
